$kind: graphql-request
name: query pastAndFutureMatches
url: "{{geql_url}}/graphql"
query: |
  query pastAndFutureMatches($days: Int!) {
      pastAndFutureMatches(days: $days) {
          PastMatches {
              id
              startDate
              startHour
          }
          FutureMatches {
              id
              startDate
              startHour
          }
      }
  }
variables: |-
  {
      "days": 1
  }
schema:
  source: auto
order: 2000
