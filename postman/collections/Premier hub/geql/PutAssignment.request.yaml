$kind: http-request
url: "{{base_url_geql_proxy}}/mocked-queries/:mockId/assignment"
method: PUT
headers:
  X-Mock-Profile: "{{X-Mock-Profile}}"
pathVariables:
  mockId: "{{mockId}}"
body:
  type: json
  content: |
    {
      "scenarioId": "basic"
    }
order: 4000
