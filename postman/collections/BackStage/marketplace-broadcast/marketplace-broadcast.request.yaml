$kind: http-request
name: marketplace-broadcast
url: https://apis.backstage.globoi.com/api/v2/marketplace-broadcast-api/premiere?filter[where][active]=true
method: GET
queryParams:
  filter[where][active]: "true"
order: 1000
